{
  "name": "jobapp",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.2.0"
  },
  "scripts": {
    "build": "npm install --prefix recipe && npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  }
}
